import React from 'react'
import { Alert } from 'react-bootstrap'

const Header = () => {
  return (
    <div>
        <Alert variant='primary' className='text-center fw-bold'>هدر</Alert>
    </div>
  )
}

export default Header